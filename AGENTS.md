# 프로젝트 작업 규칙

## 목적

이 저장소는 2026년 8월 후쿠오카·벳푸 여행에서 아로하 스타일의 프로포즈 뮤직비디오를 촬영하기 위한 모바일 우선 웹 콜시트다. 첫 화면의 우선순위는 `촬영`, `여행 동선`, `참고` 순서다.

## 사용자 경험

- 사용자에게 보이는 기본 언어는 한국어다.
- 언어를 바꾸면 메뉴뿐 아니라 일정, 촬영 지시, 반지 연출, 링크 이름, 참고 자료, 대화상자까지 한국어·영어·베트남어 전체가 바뀌어야 한다.
- 링크 URL, 예약번호, 편명, 시간과 금액은 번역하더라도 보존한다.
- 모바일에서 글자가 겹치거나 버튼이 화면 밖으로 나가지 않게 한다.
- Insta360 X2는 주요 장비지만 촬영 기법을 X2 전용으로 제한하지 않는다.
- 실제 영상 편집기를 흉내 내기보다 현장에서 빠르게 체크하고 촬영·편집 아이디어를 얻는 데 집중한다.
- 촬영 탭의 날짜별 MV 러프컷은 컷 순서, 사용 길이/박자, 다음 컷 연결법을 항상 함께 보여준다.

## 데이터와 저장

- 기본 일정과 번역은 `app.js`, 사용자 수정 촬영 목록은 `content.json`에 둔다.
- 저장되는 각 촬영 항목은 `[제목, 촬영법, 기법, 사용 길이/박자, 다음 컷 연결법]`의 5개 문자열 배열이다.
- 브라우저 수정은 `server.py`의 `/api/content`만 사용한다.
- 한 언어를 수정하면 GLM이 한국어·영어·베트남어를 모두 만들고, 세 언어 검증이 끝난 뒤 `content.json` 하나만 커밋한다.
- 원문 언어의 문장은 GLM 결과로 덮어쓰지 않는다.
- 촬영 완료 체크는 브라우저 `localStorage`에만 둔다.
- 사용자 입력은 화면에 표시할 때 `textContent` 또는 동등한 안전한 DOM API를 사용한다.

## 보안

- API 키, 편집 키, SSH 키, 토큰과 세션 값을 Git·문서·로그에 넣지 않는다.
- AWS의 기존 GLM 프록시 설정을 재사용하고 자격증명을 복사하지 않는다.
- 편집 키는 서버 환경 파일에만 두고 권한을 `600`으로 제한한다.
- 정적 파일 서버가 `.git`, 환경 파일과 숨김 경로를 제공하지 않게 유지한다.
- 서비스 주소는 사용자 요청대로 공인 IP의 HTTP `8020` 포트를 유지한다. 편집 키는 텔레그램 개인 대화로만 전달한다.

## 검증

- `node --check app.js`
- `python3 -m py_compile server.py`
- `git diff --check`
- 데스크톱과 모바일 너비에서 촬영 편집, 세 언어 전환, 영감 보드 대화상자, 날짜 접기, 여행 링크를 확인한다.
- 배포 후 `/api/health`에서 `ok`, `git`, `translation`을 확인하고 실제 Git 원격 반영 여부를 별도로 검증한다.

## 배포

- AWS 공개 서비스 포트는 `8020`이다.
- 별도 요청이 없으면 HTTPS, 도메인, 인증서, 리버스 프록시를 추가하지 않는다.
- `main`의 검증된 커밋만 배포한다.
- systemd 서비스는 저장소의 `server.py --port 8020`을 실행한다.
- 배포 전 기존 서비스를 보존하고, 실패하면 직전 검증 커밋으로 되돌릴 수 있게 한다.
